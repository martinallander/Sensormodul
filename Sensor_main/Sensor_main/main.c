#include "data_transfer.h"
#include "sensor_data.h"
/*
int main(void)
{
	initialize_all();
	Sensor_Data* current_data = create_empty_sensor(true);
	while(1)
	{
		get_angle(current_data);
		get_temp(current_data);
	}
	free(current_data);
	return 0;
}*/